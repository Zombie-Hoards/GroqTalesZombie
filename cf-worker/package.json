{
    "name": "groqtales-backend-worker",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "type-check": "tsc --noEmit",
        "db:push": "wrangler d1 execute DB --local --file=./schema.sql",
        "db:push:remote": "wrangler d1 execute DB --remote --file=./schema.sql"
    },
    "dependencies": {
        "hono": "^4.0.0",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240222.0",
        "typescript": "^5.0.4",
        "wrangler": "^3.0.0"
    }
}